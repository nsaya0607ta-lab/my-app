// 既存の Firebase Storage ルール内、match /b/{bucket}/o の配下へ追加してください。
// 本人のUIDフォルダー以外は読み書きできません。
match /users/{userId}/pdf-backup/{allPaths=**} {
  allow read, write: if request.auth != null && request.auth.uid == userId;
}
